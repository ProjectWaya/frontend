<h3 class="text-additional-info text-subtle">Question 1/2</h3>
<h2 class="h2">Where are you?</h2>

<form {{action "setInfo" on="submit"}}>

	<label>Country</label>
    {{#power-select options=countries selected=defaultCountry onchange=(action "onCountrySelected") as |country|}}
      {{country}}
    {{/power-select}}
    <label>City</label>
    {{#power-select options=cities selected=defaultCity onchange=(action "onCitySelected") as |city|}}
      {{city}}
    {{/power-select}}

    <ul>
      {{#each roles as |role|}}
          <li>
            {{radio-button
            value=role
            groupValue=currentRole
            }}
            <label for="{{role}}">{{role}}</label>
          </li>
      {{/each}}
    </ul>
    <input class="bttn bttn-action" type="submit" value="Continue"/>

</form>
