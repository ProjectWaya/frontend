{{page-navigation page='pages.index.contact.name'}}

<div class="additional-spacing-bottom text-center">
  <h2 class="h2">{{t "pages.index.contact.name"}}</h2>
</div>
<div>
	{{t "pages.index.contact.content"}}
</div>

{{outlet}}
